# Workflows Directory

This folder is intended for multi-agent execution plans, DAG models (Directed Acyclic Graphs), and reusable sequence templates.

## Structure
- Store standard multi-agent interaction recipes in JSON or YAML.
- For example, a `launch_product.yaml` might chain outputs from `seo_agent` into `website_agent`, and finally `social_media_agent`.
